{
  "private": true,
  "name": "helloworld-dev",
  "version": "0.1.0",
  "scripts": {
    "format": "prettier README.md './{docs,defaults}/**/*.{md,json}' --write",
    "services:cleanup": "sudo rm -fr ./services/*/data.tmp/"
  },
  "devDependencies": {
    "prettier": "~1.17.0"
  }
}
