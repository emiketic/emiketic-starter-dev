{
  "private": true,
  "name": "helloworld-dev",
  "version": "0.1.0",
  "scripts": {
    "format": "prettier --write '.*.{yml,json}' '*.{md,yml,json}' 'docs/**/*.{md,yml,json}' 'services/*/*.{md,yml,json}'",
    "services:cleanup": "sudo rm -fr ./services/*/data.tmp/"
  },
  "devDependencies": {
    "prettier": "~1.18.2"
  }
}
